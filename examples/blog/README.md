# Blog example

Demonstrates hasMany, belongsTo, belongsToMany, hidden/computed fields, JSON metadata, FTS5 search, and relation counts.

Run from the repository root:

```bash
node examples/blog/index.js
```

Use a persistent SQLite file:

```bash
LITEORM_DB=./blog.sqlite node examples/blog/index.js
```
