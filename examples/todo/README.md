# Todo example

Demonstrates schema builder, models, validation, scopes, transactions, JSON fields, query builder filters, and soft deletes.

Run from the repository root:

```bash
node examples/todo/index.js
```

Use a persistent SQLite file:

```bash
LITEORM_DB=./todo.sqlite node examples/todo/index.js
```
